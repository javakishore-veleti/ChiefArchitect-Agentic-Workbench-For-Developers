# Angular shared configuration

Copy the configuration and vocabulary examples, then commit organization-safe mappings. Keep credentials outside Git and store only secret references. `ANGULAR_CONFIG_OVERRIDE_URI` can select a local/file URI, HTTPS URL, `s3://` object, or `azblob://account/container/blob`; named override entries replace complete base entries.
