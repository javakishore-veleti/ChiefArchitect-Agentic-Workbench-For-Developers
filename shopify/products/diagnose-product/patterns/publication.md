# Publication and channel visibility

Product status, publication membership, market/catalog eligibility and Storefront API visibility are separate. Query publications and publication state using only fields allowed by the app's scopes; a requested response field can cause access denied even if the mutation itself is permitted.

Check the exact sales channel used by the target Hydrogen app and environment. Never publish as a diagnostic probe. Report which visibility gate lacks evidence.
