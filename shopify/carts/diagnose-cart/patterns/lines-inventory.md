# Lines and inventory

Compare requested merchandise/quantity/selling plan with returned lines. Use cart-line IDs for update/remove, not variant IDs. Confirm publication, market availability, inventory policy, quantity rules, and parent/component relationships. Capture warnings because Shopify can adjust quantities or omit unavailable merchandise without a transport failure. For apparent stale counts, re-query the same complete cart ID before blaming inventory.

