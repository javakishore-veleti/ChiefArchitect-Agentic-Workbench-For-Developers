# API version and schema

## Match signals

- deprecated
- version
- undefined field
- null after upgrade
- schema

## Procedure

Compare the same minimal query across configured versions or environments and inspect schema deprecation metadata.

Resolve topology using `references/environment-topology.md`. Apply `references/evidence-and-safety.md`. Load or run only these relevant artifacts:

- `scripts/compare-graphql.mjs`
