import {execFileSync} from 'node:child_process'; export async function get(ref){if(!ref.name||!ref.namespace||!ref.key)throw new Error('kubernetes-secret requires name, namespace and key');try{const encoded=execFileSync('kubectl',['get','secret',ref.name,'--namespace',ref.namespace,'--output','jsonpath={.data.'+ref.key+'}'],{encoding:'utf8',stdio:['ignore','pipe','ignore']}).trim();return Buffer.from(encoded,'base64').toString('utf8')}catch{throw new Error('kubernetes secret resolution failed')}}
