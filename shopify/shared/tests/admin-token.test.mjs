import test from 'node:test';import assert from 'node:assert/strict';import {getAdminToken,clearTokenCache} from '../auth/get-admin-token.mjs';test('exchanges client credentials and caches token',async()=>{clearTokenCache();let calls=0;const fetchImpl=async(url,request)=>{calls++;assert.match(url,/admin\/oauth\/access_token$/);assert.match(request.body.toString(),/grant_type=client_credentials/);return{ok:true,status:200,json:async()=>({access_token:'synthetic-token',expires_in:86399})}};const secretResolver=async ref=>ref.testValue;const input={shopDomain:'example.myshopify.com',authentication:{mode:'client-credentials','client-id':{testValue:'id'},'client-secret':{testValue:'secret'}}};assert.equal(await getAdminToken(input,{fetchImpl,secretResolver}),'synthetic-token');assert.equal(await getAdminToken(input,{fetchImpl,secretResolver}),'synthetic-token');assert.equal(calls,1)})
