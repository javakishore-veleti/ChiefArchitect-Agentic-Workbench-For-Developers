import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs/promises';import os from 'node:os';import path from 'node:path';import {resolveSecret} from '../secrets/resolve-secret.mjs';test('environment provider',async()=>{process.env.TEST_SHOPIFY_SECRET='synthetic';assert.equal(await resolveSecret({provider:'environment',variable:'TEST_SHOPIFY_SECRET'}),'synthetic');delete process.env.TEST_SHOPIFY_SECRET});test('mounted file provider',async()=>{const d=await fs.mkdtemp(path.join(os.tmpdir(),'shopify-secret-'));const f=path.join(d,'value');await fs.writeFile(f,'synthetic-file\n');assert.equal(await resolveSecret({provider:'mounted-file',path:f}),'synthetic-file');await fs.rm(d,{recursive:true})})
