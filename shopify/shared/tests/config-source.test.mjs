import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs/promises';import os from 'node:os';import path from 'node:path';import {loadShopifyConfig,mergeConfigs} from '../config/load-config.mjs';const base={'schema-version':1,'configs-envs-mapping':[{'config-name':'a',envs:['dev']}],configs:[{'config-name':'a',storefront:'one',shopify:{'shop-domain':'one.myshopify.com','admin-api-version':'2026-04'},'hydrogen-apps':[]}]};const override={'schema-version':1,'configs-envs-mapping':[{'config-name':'a',envs:['qa']}],configs:[{'config-name':'a',storefront:'one',shopify:{'shop-domain':'qa.myshopify.com','admin-api-version':'2026-04'},'hydrogen-apps':[]}]};test('override replaces whole entries by config-name',()=>{const x=mergeConfigs(base,override);assert.deepEqual(x['configs-envs-mapping'][0].envs,['qa']);assert.equal(x.configs[0].shopify['shop-domain'],'qa.myshopify.com')});test('loads local override through predefined source mechanism',async()=>{const d=await fs.mkdtemp(path.join(os.tmpdir(),'shopify-config-test-')),b=path.join(d,'base.json'),o=path.join(d,'override.json');await fs.writeFile(b,JSON.stringify(base));await fs.writeFile(o,JSON.stringify(override));const x=await loadShopifyConfig({basePath:b,overrideUri:o});assert.equal(x.configs[0].shopify['shop-domain'],'qa.myshopify.com');await fs.rm(d,{recursive:true})})
