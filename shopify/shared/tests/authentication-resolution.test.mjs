import test from 'node:test';import assert from 'node:assert/strict';import {resolveAuthentication} from '../auth/resolve-authentication.mjs';const base={storefront:'storefront-a',environment:'qa',shopify:{}};test('scoped access token is default',()=>{const a=resolveAuthentication(base,{SHOPIFY_STOREFRONT_A_QA_ADMIN_ACCESS_TOKEN:'x'});assert.equal(a.mode,'runtime-access-token');assert.equal(a['access-token'].variable,'SHOPIFY_STOREFRONT_A_QA_ADMIN_ACCESS_TOKEN')});test('scoped client credentials are default',()=>{const a=resolveAuthentication(base,{SHOPIFY_STOREFRONT_A_QA_CLIENT_ID:'x',SHOPIFY_STOREFRONT_A_QA_CLIENT_SECRET:'y'});assert.equal(a.mode,'client-credentials')});test('custom common prefix is supported',()=>{const a=resolveAuthentication({...base,'environment-variable-prefix':'ENTERPRISE_SHOPIFY_'},{ENTERPRISE_SHOPIFY_STOREFRONT_A_QA_ADMIN_ACCESS_TOKEN:'x'});assert.equal(a['access-token'].variable,'ENTERPRISE_SHOPIFY_STOREFRONT_A_QA_ADMIN_ACCESS_TOKEN')});test('explicit provider wins',()=>{const configured={mode:'offline-access-token','access-token':{provider:'mounted-file',path:'/secret'}};assert.equal(resolveAuthentication({...base,shopify:{authentication:configured}},{SHOPIFY_STOREFRONT_A_QA_ADMIN_ACCESS_TOKEN:'x'}),configured)});test('never falls back across scopes',()=>assert.throws(()=>resolveAuthentication(base,{SHOPIFY_STOREFRONT_A_PRODUCTION_ADMIN_ACCESS_TOKEN:'x'}),/no authentication configured/))
